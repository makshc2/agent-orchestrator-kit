# Tasks — fix-metrics-model-and-spend

## 1. CLI: модель, платформа, таблиця metrics (change-metrics)

- [x] 1.1 Резолв моделі `--model` / `AOK_MODEL`
  Files: bin/agent-orchestrator.js
  Do: додати `resolveModel(opts, env)`, що повертає непорожній trim `--model`, інакше непорожній trim `process.env.AOK_MODEL`, інакше `null` (порожній рядок = `null`; без таксономії і без lowercasing); у persist-гілці `handoff` при `opts.metrics !== false` передати результат у `metricsRecordSessionEnd` як `model` замість сирого `opts.model`; якщо результат `null` — надрукувати попередження в stderr через `console.error` (підказка передати `--model <llm-product-id>` або `AOK_MODEL`; не використовувати `log.warn`, бо він пише в `console.log`/stdout і зіпсує next-thread prompt); не викликати `process.exitCode = 1` через відсутню модель
  Done-when: коли `sources` порожні, `handoff <name> --model claude-opus-5` пише `sessions[0].model === "claude-opus-5"`; коли `sources` порожні, `AOK_MODEL=claude-fable-5` без прапорця пише `claude-fable-5`; коли `sources` порожні, persist без прапорця й env пише `model: null`, exit 0, stderr містить слово `model`

- [x] 1.2 Резолв платформи `--platform` / `AOK_PLATFORM`
  Files: bin/agent-orchestrator.js
  Do: додати `VALID_PLATFORMS = new Set(['cursor','claude','amp'])` і `resolvePlatform(opts, env)` з пріоритетом `--platform` (lowercase; значення поза сетом → `{ error: 'invalid --platform (use cursor, claude, or amp)' }`) → `AOK_PLATFORM` (якщо в сеті — value; якщо непорожній і не в сеті — `{ value: null, warn }` без fail) → `{ value: null }`; додати опцію `--platform <platform>` до команди `handoff`; у persist при записі сесії зберегти `platform: resolved || null` у об'єкті, який пушиться в `metrics.sessions`; при `error` — `log.err`, `process.exitCode = 1` і `return` (як `applyRuntimeToFields`: не продовжувати persist і не викликати `metricsRecordSessionEnd`); при `warn` — `console.error`; не читати `CURSOR_AGENT` і не додавати константу env-маркерів платформи
  Done-when: persist `--platform cursor` пише `sessions[0].platform === "cursor"`; `AOK_PLATFORM=amp` без прапорця пише `amp`; `--platform foo` дає exit ≠ 0 і не створює сесію з `platform: "foo"`; без прапорця/env і з `CURSOR_AGENT=1` — `platform: null`, exit 0

- [x] 1.3 Людська таблиця `metrics`: колонки `roles` і `models`
  Files: bin/agent-orchestrator.js
  Do: у команді `metrics` без `--json` замінити заголовок рядка фаз `phase      sessions  time      tokens    cost      agents` на рядок з колонками `roles` (значення `phase.agents.join(', ') || '—'`) і `models` (значення `phase.models.join(', ') || '—'`); не залишати заголовок `agents`, який друкує Closed role; у `recent sessions` лишити `role` і `[model]`; оновити `.description` команди `metrics` так, щоб воно називало roles і models окремо, не «agents, models» як одне
  Done-when: `metrics <name>` для сесії `role: Architect`, `model: claude-opus-5` друкує заголовок `models` і `claude-opus-5` у колонці models; `Architect` є в колонці `roles` і відсутній як єдине значення колонки `models`

## 2. Archive завжди фіналізує metrics (change-metrics, lean-archive)

- [x] 2.1 `metricsFinalizeArchive` створює файл і сесію Archiver
  Files: bin/agent-orchestrator.js
  Do: додати до команди `archive` опції `--model <name>` і `--platform <platform>`; на старті action (до гейтів і `renameSync`) викликати `resolvePlatform(opts, env)` — при `error` зробити `log.err` і `process.exitCode = 1` і return, файли не чіпати; `resolveModel` зберегти в локальну змінну; прибрати `if (!existsSync(filePath)) return null` у `metricsFinalizeArchive`; завжди `loadMetricsFile`; додати в `sessions` один запис `{ startedAt: nowIso, endedAt: nowIso, durationMs: null, role: 'Archiver', phase: phaseForRole('Archiver'), runtime і agentId з уже резолвлених полів archive, model з попередньо резолвленого resolveModel, platform з резолвленого value або null, tasks з progress, inputTokens/outputTokens/totalTokens/costUsd: null }`; виставити `archivedAt`, `pending: null`, `updatedAt`, викликати `recomputeMetricsAggregates`, зберегти, повернути шлях; після фінального `recomputeMetricsAggregates` якщо `metrics.spend.costUsd === null` — `console.error` warning у stderr що USD відсутній (Amp credits і заповнені токени не скасовують warning; не перевіряти `metrics.sessions.length === 0` і не перевіряти всі ключі `METRICS_SPEND_KEYS` у `metrics.spend`); рядок stdout `metrics:` друкувати завжди після успішного finalize
  Done-when: `archive <name>` без попереднього `metrics.json` створює архівний файл з непорожнім `archivedAt`, `pending: null`, сесією `role: Archiver` / `phase: archive` / `durationMs: null` / null spend; stderr містить warning; exit 0; наявний файл отримує `archivedAt` і додаткову сесію Archiver; `archive <name> --platform foo` дає exit ≠ 0 і не переміщує change

## 3. Протокол Session Exit (session-handoff)

- [x] 3.1 Rule, skill і субагент вимагають `--model` і описують auto-collect
  Files: templates/.agents/rules/session-handoff.mdc, templates/.agents/skills/agent-orchestration/SKILL.md, templates/.agents/subagents/session-handoff.md
  Do: у `session-handoff.mdc` крок persist замінити на обов'язкове `--model <llm-product-id>` продукту цього чату (приклади `claude-opus-5`, `claude-fable-5`, `gpt-5.6-sol`, `cursor-grok-4.6`) плюс заборону передавати Closed role (`Architect`, `Implementer`, `Explorer`) або ім'я субагента (`spec-architect`, `session-handoff`) як `--model`; явно написати що persist auto-collect локальний usage з Claude JSONL, Amp threads і Cursor vscdb; батько SHOULD все одно передати `--model`; батько MUST NOT вгадувати токени; `--input-tokens` / `--output-tokens` / `--total-tokens` / `--cost-usd` override лише session-level totals і не витирають карти платформ; прибрати primary path «немає скрейперів / spend лише коли UI платформи показав числа»; опційно `--platform cursor|claude|amp` або `AOK_PLATFORM`; явна фраза що той самий `npx agent-orchestrator-kit handoff <name>` працює в Cursor, Claude Code і Amp і MUST NOT вимагати Cursor SDK, парсер Claude `/cost` або Amp billing API як обов'язковий крок; у SKILL.md додати той самий контракт у блок «End of each session» крок 2 і в абзац «Before declaring a session closed»; у `session-handoff.md` Persist mode крок 2 додати `--model` / заборону ролі / auto-collect / override totals / опційний `--platform`
  Done-when: усі три файли містять `--model`, заборону ролі/субагента як моделі, згадку auto-collect (Claude JSONL / Amp threads / Cursor vscdb), заборону вгадувати токени, згадку що прапорці override totals, згадку Cursor/Claude/Amp і не вимагають Cursor SDK, Claude `/cost` або Amp billing як обов'язковий крок; жоден не формулює primary path як «spend лише коли платформа показала числа»

- [x] 3.2 opsx-archive згадує фіналізацію metrics
  Files: templates/.agents/commands/opsx-archive.md
  Do: у кроці «Run the CLI» після опису фінального `handoff.md` додати одне речення: успішний `archive` завжди створює або оновлює `metrics.json` (`archivedAt`, сесія Archiver) і запускає collect, якщо немає `--no-collect`; якщо `spend.costUsd` є `null` — warning у stderr, не провал гейта; не додавати ручних кроків merge/move і не спавнити субагента; після правки файл лишається ≤ 1536 байт (1.5 KB)
  Done-when: `.agents/commands/opsx-archive.md` після init/update містить слово `metrics.json`, не містить «spawn spec-archiver», розмір файлу ≤ 1536 байт

## 4. Тести та документація

- [x] 4.1 Смоук-тести моделі, платформи, таблиці й archive-finalize
  Files: test/smoke.test.js
  Do: розширити наявні тести метрик (блок близько рядків 1311–1406) і archive (блок близько 1664–1694) плюс нові `test(...)` у tmp-git-репо з `METRICS_HANDOFF`: (а) persist `--model claude-opus-5` і окремо `AOK_MODEL=gpt-5.6-sol` без прапорця пишуть відповідний `session.model` коли sources порожні; persist без обох — `model: null`, exit 0, combined stdout+stderr містить `model`; (б) `--platform cursor` пише `platform: "cursor"`; `AOK_PLATFORM=amp` пише `amp`; `--platform foo` non-zero; `CURSOR_AGENT=1` без flag/env лишає `platform: null`; (в) `metrics <name>` містить заголовок `models` і `claude-opus-5`, не друкує `Architect` як значення колонки models; (г) наявний тест `archive --sync` оновити: після archive є `archivedAt`, `pending: null`, сесія `Implementer`/`apply` збережена і додана сесія `Archiver`/`archive`; (д) новий тест: fixture без `metrics.json` — після `archive` файл існує, є Archiver, warning у виводі, exit 0; наявні кейси `--no-metrics` і null-honest spend у tmp без фікстур адаптерів лишити зеленими; не читати реальний `~/.claude` / Amp / Cursor home розробника
  Done-when: `npm test` зелений локально; нові асерти з пунктів (а)–(д) присутні в `test/smoke.test.js`

- [x] 4.2 README і CHANGELOG
  Files: README.md, CHANGELOG.md
  Do: у README секції `### Change metrics` (близько рядка 800) записати: `session.model` = LLM product id (primary з sources або `--model` → `AOK_MODEL` → `null` з warning у stderr); persist/archive auto-collect локальний usage з Claude JSONL, Amp threads, Cursor vscdb; прапорці `--input-tokens` / `--output-tokens` / `--total-tokens` / `--cost-usd` override лише session totals і не витирають `spendByPlatform` / `spendByModel`; never guess; never invent USD з Amp credits; never Cursor SDK / npm sqlite / pricing table; колонки людської таблиці `roles` (Closed role) і `models` (LLM id) плюс таблиці by platform і by model без єдиного total $ з різних одиниць; `archive` завжди створює/фіналізує `metrics.json` і додає сесію Archiver; опційні `--platform` / `AOK_PLATFORM` (`cursor|claude|amp`); `--no-metrics` лишається opt-out; `--no-collect` пропускає адаптери; `gate-check` не вимагає файл; оновити приклад `metrics add-thing` коментар з `agents` на `roles`/`models`; у CHANGELOG секцію `[Unreleased]` додати `### Changed` запис про model-resolution, auto-collect окремо по платформах/моделях, колонку models, archive-always-finalize і протокол `--model`
  Done-when: README називає ланцюжок `--model`/`AOK_MODEL`, auto-collect, колонки `roles` і `models`, таблиці platform/model, фіналізацію archive, `--platform` і `--no-collect`; CHANGELOG `[Unreleased]` містить цей запис

## 5. Spend collectors

- [x] 5.1 Claude-адаптер на jsonl-фікстурі
  Files: new file: bin/spend-collect.js
  Do: створити ESM-модуль `bin/spend-collect.js` з експортом `collectSpend({ cwd, windowStart, windowEnd, existingSourceIds, env, homedir })`, що повертає `{ sources, byPlatform, byModel, notes }` (і внутрішнім `collectClaude`); якщо аргумент `cwd` не передано — брати `process.cwd()`; читати `$HOME/.claude/projects/<cwd-encoded>/*.jsonl`, де cwd-encoded будується з цього `cwd` заміною кожного `/` і кожного `.` на `-` (приклад: `/home/shevyakov.m/WebstormProjects/app` → `-home-shevyakov-m-WebstormProjects-app`); парсити JSONL-рядки assistant з `message.usage` (`input_tokens`, `output_tokens`, `cache_*` додавати до input якщо є) і `message.model`; `costUsd` лише з `total_cost_usd`/аналога на записі інакше `null`; id = `message.id`; `byPlatform.claude.source = "claude-jsonl"`; кожен елемент `sources` = `{ id, platform, model, inputTokens, outputTokens, totalTokens, costUsd, ampCredits, at }`; фільтр вікна за полем рядка `timestamp` (не інше поле); фільтр проєкту: поле рядка `cwd` === переданий аргумент `cwd`; пропуск id з `existingSourceIds`; не постачати pricing table; не ходити в мережу
  Done-when: виклик `collectSpend` з явним `cwd` (не обов'язково `process.cwd()` кіт-репо) і `homedir`/`env.HOME` на tmp-фікстурі в `$HOME/.claude/projects/<cwd-encoded>/`, де jsonl має assistant `message.id=msg-1`, model `claude-opus-4-7`, `input_tokens: 10`, `output_tokens: 5`, поле `cwd` рівне переданому `cwd` і поле `timestamp` у вікні, повертає `sources[0]` з цими токенами і `platform: "claude"`; `byPlatform.claude.source === "claude-jsonl"`; `costUsd` є `null` якщо `total_cost_usd` немає

- [x] 5.2 Amp-адаптер на thread-фікстурі
  Files: new file: bin/spend-collect.js
  Do: у тому ж модулі додати `collectAmp`: корінь `env.AMP_DATA_DIR` або `env.XDG_DATA_HOME/amp` або `$HOME/.local/share/amp`; читати `threads/*.json`; проєктний match: порівняти аргумент `collectSpend` `cwd` (або `process.cwd()`, якщо `cwd` не передано) з кожним `env.initial.trees[].uri` після зняття префікса `file://` (типово третій слеш: `file:///home/...` → `/home/...`); якщо хоча б одне дерево збігається — включити thread; якщо `env.initial.trees` відсутній або порожній — пропустити thread; не читати і не вигадувати поля `cwd` / `meta.cwd`; `inputTokens` = `usage.totalInputTokens` якщо поле є, інакше `usage.inputTokens` плюс `cacheCreationInputTokens` і `cacheReadInputTokens` якщо вони є; також брати `usage.model`, `usage.outputTokens`, `usage.timestamp`; id = `messageId` або `toMessageId`; `byPlatform.amp.source = "amp-thread"`; `ledger.jsonl` опційний і може бути відсутнім — не вимагати файл і не вимагати реальних `ampCredits`; якщо ledger немає — `ampCredits: null`; ніколи не писати credits у `costUsd`; вікно за `usage.timestamp`; dedup за id як у 5.1
  Done-when: `collectSpend` з явним `cwd` і `AMP_DATA_DIR` на tmp-thread, де assistant usage `inputTokens: 8`, `outputTokens: 2`, model `amp-model`, `usage.timestamp` у вікні, `env.initial.trees` містить `uri` рівний `file://` + цей `cwd`, повертає `platform: "amp"`, токени 8/2, `costUsd: null`, `ampCredits: null`; thread без `trees` не потрапляє в sources; thread з іншим `trees[].uri` не потрапляє; фікстура не містить `meta.cwd`

- [x] 5.3 Cursor-адаптер: snapshot vscdb або порожньо
  Files: new file: bin/spend-collect.js
  Do: додати `collectCursor`: не парсити `agent-transcripts/*.jsonl` як usage; знайти DB за Linux `$XDG_CONFIG_HOME/Cursor/User/globalStorage/state.vscdb` (fallback `$HOME/.config/...`; macOS Application Support; Windows APPDATA); snapshot-copy файлу і `-wal`/`-shm` якщо є у tmp; читати системним `sqlite3` через `execFile`/`execSync` без npm sqlite; якщо бінаря/DB немає або схема без token-полів — повернути порожні sources, `source: "none"`, рядок `note`; не оцінювати токени з `text.length`; не чіпати cookies і Cursor SDK
  Done-when: виклик без sqlite3 або без DB повертає `sources: []` і note, не кидає; виклик з tmp, де немає token-колонок, теж порожній; жоден код адаптера не імпортує `better-sqlite3` / `sql.js` / `ccusage`

- [x] 5.4 Підключити collect до persist і archive
  Files: bin/agent-orchestrator.js, new file: bin/spend-collect.js
  Do: `import { collectSpend } from './spend-collect.js'`; у `defaultMetrics`/`loadMetricsFile` додати `spendByPlatform` (три ключі з null-полями і `source: "none"`) і `spendByModel: []`; на persist при `opts.metrics !== false` і `opts.collect !== false` викликати collect з вікном `[pending.startedAt || last session.endedAt || createdAt, endedAt]`, `existingSourceIds` з усіх `session.sources`, `cwd` передати як `process.cwd()` у CLI; записати `session.sources`; виставити `session.model` як primary з sources (найбільший `totalTokens`, при рівності platform потім id) або D2 якщо sources порожні (warning D2 лише якщо фінальний model null); якщо унікальних model id у sources більше одного — записати `session.models` як масив цих унікальних id; якщо немає жодного з прапорців `--input-tokens`/`--output-tokens`/`--total-tokens`/`--cost-usd` — записати session `inputTokens`/`outputTokens`/`totalTokens`/`costUsd` як агрегат `sources` цієї сесії (сума токенів; `costUsd` лише з рядків де `costUsd !== null`; `ampCredits` ніколи не входять у `costUsd`; якщо жодного USD-рядка — `costUsd: null`); якщо є хоча б один з цих прапорців — записати session totals з прапорців (немає `--total-tokens` при наявному input або output → сума наявних override) і не чистити `sources`/`spendByPlatform`/`spendByModel`/`session.models`; змінити `recomputeMetricsAggregates` так, що `spend.costUsd` = null-honest сума по сесіях: якщо `session.costUsd !== null` — це значення, інакше сума не-null `source.costUsd` цієї сесії (не додавати source USD поверх уже заповненого `session.costUsd`); `spend.inputTokens`/`outputTokens`/`totalTokens` так само (session поле або сума sources); `spendByPlatform` і `spendByModel` перерахувати з усіх `session.sources` (costUsd лише USD, ampCredits окремо); на archive finalize при `opts.collect !== false` викликати collect з вікном `[last session.endedAt || createdAt, now]` і тим самим правилом totals/`models` для сесії Archiver; додати `--no-collect` до `handoff` і `archive`; `--no-metrics` не викликає collect; порожній collect не ставить `process.exitCode`
  Done-when: persist у tmp з Claude-фікстурою без `--cost-usd` і без інших spend-прапорців пише `sessions[0].sources` і `spendByPlatform.claude`; `sessions[0].totalTokens` дорівнює сумі `totalTokens` цих sources; `sessions[0].costUsd` дорівнює `total_cost_usd` запису або `null` якщо поля немає; той самий persist з `--cost-usd 9.99` лишає карту claude і `sources`, ставить `sessions[0].costUsd === 9.99`, не чистить maps; якщо sources мають два різні model id — `session.models` містить обидва і `session.model` є primary; `spend.costUsd` у файлі бере USD з session або з sources за правилом Do; persist `--no-collect` при наявній фікстурі пише `sources: []`; archive `--no-collect` фіналізує файл без sources Archiver; persist без фікстур exit 0

- [x] 5.5 Таблиці metrics by platform і by model
  Files: bin/agent-orchestrator.js
  Do: у людському виводі `metrics` без `--json` після таблиці фаз надрукувати таблицю by platform (рядки cursor/claude/amp: tokens, costUsd, ampCredits, source) і таблицю by model (model, platform, tokens, costUsd, ampCredits); рядок підсумку `cost` брати лише з `spend.costUsd`; не друкувати одне число, що є сумою Amp credits + USD; `--json` лишає сирий об'єкт з новими полями
  Done-when: `metrics <name>` на файлі з `spendByPlatform.claude.costUsd=1.5` і `spendByPlatform.amp.ampCredits=20` містить заголовки platform і model (або еквівалентні українські/англійські підписи таблиць) і не містить єдиного total, рівного `21.5`

- [x] 5.6 Юніт-тести collect на tmp-фікстурах
  Files: new file: test/spend-collect.test.js, test/smoke.test.js
  Do: додати `test/spend-collect.test.js` під `node --test`: кожен тест ставить `HOME`/`AMP_DATA_DIR`/`XDG_CONFIG_HOME` на власний tmp і передає явний `cwd` плюс `homedir`/`env` у `collectSpend` (не хардкодити `process.cwd()` кіт-репо); кейси claude jsonl (вікно за полем `timestamp`, cwd-mismatch проти переданого `cwd`, encode `/` і `.` у імені папки, `cache_*` у input, відсутній `total_cost_usd` → null, dedup existingSourceIds); amp thread match через `env.initial.trees[].uri` після strip `file://`, thread без trees пропускається, фікстура без `meta.cwd`, відсутній `ledger.jsonl` → `ampCredits: null` і `costUsd: null`, `inputTokens` з `totalInputTokens` якщо поле є; cursor skip без sqlite/DB; не робити `readdir`/`readFile` реального `join(os.homedir(), '.claude')` у CI; у `test/smoke.test.js` додати persist Claude-фікстури без `--cost-usd` (`sessions[0].totalTokens` дорівнює сумі source tokens; `costUsd` з `total_cost_usd` або `null`), persist `--no-collect` і людський `metrics` з таблицями platform/model на підставному `metrics.json`
  Done-when: `npm test` зелений; `test/spend-collect.test.js` існує і не містить літералів шляхів `/home/` розробника як джерела фікстур; smoke асертить persist Claude-фікстури без `--cost-usd` (`sessions[0].totalTokens` == source tokens), `--no-collect` і наявність platform/model таблиць у stdout `metrics`
