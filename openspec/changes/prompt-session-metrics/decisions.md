# Decisions — prompt-session-metrics

<!-- append-only; пише npx agent-orchestrator-kit handoff <name> з handoff.md ## Decisions -->

- 2026-08-29 session-metrics-source: primary джерело spend — секція `## Metrics` у handoff.md, яку заповнює агент на виході сесії, бо саме він знає модель, платформу і витрати; адаптери демотовані до opt-in
- 2026-08-29 session-metrics-missing: відсутній самозвіт дає warning плюс `spendSource: "unreported"` і exit 0, а не fail persist — гейт persist на числа є явним non-goal, і незакрита сесія ламає `pending`
- 2026-08-29 session-metrics-collect-flag: `--no-collect` видаляється, натомість вводиться opt-in `--collect` на `handoff` і `archive` (BREAKING)
- 2026-08-29 session-metrics-hook: `ensureCursorSpendHook` лишається лише в `init`/`update`/`sync`/`mcp-setup`; self-heal у persist і restore прибирається
- 2026-08-29 session-metrics-archive: фінальна зводка archive рендериться тією самою функцією, що й `metrics <name>`, щоб два виводи не розходились
- 2026-08-30 review-tier1-block: Tier 2 не спавниться до зеленого `gate-check --review`; повернення на фазу Architect, а не латання `proposal.md` у сесії рев'ю — фаза рев'ю read-only щодо артефактів пропозиції
- 2026-08-30 propose-scope-minimal: фаза propose обмежилась двома відсутніми секціями `proposal.md`; `design.md`, `tasks.md` і delta-спеки не переписувались, бо strict-валідація і контракт тасків були зелені ще до сесії
- 2026-08-30 proposal-nongoals-mirror: `## Non-goals` у `proposal.md` дзеркалить `design.md` § `Goals / Non-Goals`; при майбутніх розбіжностях джерелом лишається `design.md`, а `proposal.md` підганяється під нього
- 2026-08-30 acceptance-criteria-observable: критерії приймання формулюються як спостережувана поведінка CLI (exit code, вміст `metrics.json`, розділення stdout/stderr), а не як опис внутрішньої реалізації — щоб Tier 2 і Implementer перевіряли одне й те саме
- 2026-08-30 spec-architect-inline-body: за відсутності `.cursor/agents/spec-architect.md` спеціаліст спавниться з тілом ролі з `templates/.agents/subagents/spec-architect.md`; кондуктор не виконує роботу спеціаліста в батьківському треді
- 2026-08-30 review-verdict-request-changes: Tier 2 returned REQUEST CHANGES on two blocking findings; the change goes back to Architect and MUST NOT enter apply until both are resolved and a new review session records APPROVE
- 2026-08-30 lean-archive-delta-required: capability `lean-archive` duplicates archive-metrics requirements and must get its own delta in this change (MODIFIED and/or REMOVED plus a Migration note) with matching lines in `proposal.md` Capabilities and Impact; leaving it untouched would make `openspec/specs/` self-contradictory after sync
- 2026-08-30 no-collect-survivors-must-be-deltaed: every surviving main-spec requirement that references `--no-collect` or collect-by-default (`change-metrics` § `--no-metrics пропускає запис сесії і collect`) must be added to the delta before apply — deleting the flag in code while the spec still mandates it is the defect class Tier 2 exists to catch
- 2026-08-30 metrics-writeback-ownership-open: the contradiction between "the `## Metrics` section mirrors `metrics.json`" (both deltas) and D5's write order (handoff.md before `metricsRecordSessionEnd`) is unresolved; Architect must either hoist resolution ahead of `buildHandoffMarkdown` or narrow the spec sentence to "self-report as written + `unknown` skeleton"
- 2026-08-30 spec-reviewer-inline-body: with no `.cursor/agents/spec-reviewer.md`, the specialist is spawned as an isolated subagent carrying the role body from `templates/.agents/subagents/spec-reviewer.md`; the conductor verifies the report and does not perform the specialist's work
- 2026-08-30 metrics-writeback-self-report-only: закриває `metrics-writeback-ownership-open` — обрано варіант (b): `## Metrics` у `handoff.md` лишається самозвітом як його написав агент плюс `unknown` для порожніх полів, CLI не переписує секцію резолвленими значеннями; джерело істини про те, що потрапило в сесію, — `metrics.json`. Порядок persist з D5 (запис `handoff.md` перед `metricsRecordSessionEnd`) лишається без змін, розбіжність файлу і `metrics.json` при `--input-tokens` / невалідній платформі є очікуваною і покрита сценарієм
- 2026-08-30 lean-archive-owns-gates-not-metrics: `lean-archive` віддає archive-метрики capability `change-metrics` через REMOVED «Archive завжди фіналізує metrics.json» з Migration; натомість ADDED «Гейті archive не залежать від metrics.json» зберігає archive-flow твердження (metrics не гейт, невалідний `--platform` відхиляється до move), які інакше зникли б разом із видаленою вимогою
- 2026-08-30 total-tokens-single-owner: суму `input + output` для `totalTokens` рахує лише `resolveSessionSpend`; `parseMetricsSection` повертає `totalTokens` тільки коли ключ `total_tokens` реально є в секції — щоб сума не застосовувалась двічі
- 2026-08-30 legacy-spend-source-display-layer: дефолт `spendSource: "unreported"` для legacy-записів сесій живе в шарі відображення (`renderMetricsSummary`), `loadMetricsFile` записів не нормалізує і файл на диску не мігрує
- 2026-08-30 2026-08-30 metrics-writeback-self-report-only: closes `metrics-writeback-ownership-open` — the `## Metrics` section stays the agent's self-report plus an `unknown` skeleton; the CLI MUST NOT overwrite it with resolved values, and `metrics.json` is the sole source of truth for what landed in the session
- 2026-08-30 2026-08-30 lean-archive-owns-gates-not-metrics: `lean-archive` gives up archive-metrics semantics entirely (REMOVED with Migration) and keeps only that `metrics.json` and the self-report are not archive gates and that an invalid `--platform` is rejected before move; `change-metrics` is the single owner of archive-metrics
- 2026-08-30 2026-08-30 total-tokens-single-owner: `resolveSessionSpend` is the single owner of the `total_tokens` sum rule so it is never applied twice; the parser no longer computes it
- 2026-08-30 2026-08-30 legacy-spend-source-display-layer: the legacy `spendSource: "unreported"` / `ampCredits: null` default is applied at the display layer (`renderMetricsSummary`), not at `loadMetricsFile`
- 2026-08-30 2026-08-30 architect-fix-round-scope: the Architect fix round addressed H1, H2, M1, M2, M3 and all four L findings; `review.md` was left untouched because a new review session writes a new verdict
- 2026-08-30 review-verdict-approve: re-review APPROVE; H1, H2, M1, M2 and M3 are resolved; apply is unblocked
- 2026-08-30 apply-notes-archive-wipe: implementer MUST carry `priorFields.metrics` into the archive `fields` object before the post-move rewrite at `bin/agent-orchestrator.js:3258` (finding M-A)
- 2026-08-30 apply-notes-warnings-stderr: `parseMetricsSection` warnings MUST be printed on stderr in persist (finding M-B); the invalid-platform warning already prints via `resolvePlatform` at line 3620 — do not print it twice
- 2026-08-30 apply-complete-28-28: all 28 tasks implemented and checked; local verify is green
- 2026-08-30 apply-archive-metrics-carried: archive writes `fields.metrics` from `priorFields` before the post-move `handoff.md` rewrite so the Archiver session can resolve the self-report
- 2026-08-30 apply-collect-opt-in: persist and archive call collect/backfill only when `opts.collect === true`
