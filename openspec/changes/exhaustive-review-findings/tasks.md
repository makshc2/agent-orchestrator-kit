## 1. Review templates

- [x] 1.1 Вичерпний Tier 2 і схема в opsx-review.md
  Files: templates/.agents/commands/opsx-review.md
  Do: У кроці «If Tier 1 fails» замінити `(source: gate-check)` на точний рядок `**Source:** gate-check` і явно написати, що T1 `review.md` не має секції `## Checklist`; додати одне речення, що цей parent-запис T1 `review.md` є accepted exception до pipeline-subagents «parent MUST NOT сама писати вердикт»; семантику «do NOT spawn `spec-reviewer`» і «do NOT read the artifacts» не змінювати. У кроці spawn вставити: parent MUST paste the full Tier 2 checklist from this file into the `spec-reviewer` prompt, including the Vue 3 items when `project.stack: vue3`; перед спавном parent MUST зафіксувати, чи існував `openspec/changes/<name>/review.md`, і передати цей факт плюс шлях у spawn-промпт. Додати правила: MUST NOT stop at the first blocking finding; finish the full LLM checklist and a complete scan of proposal.md, design.md, tasks.md, all delta specs, and referenced main specs/repo paths before writing the verdict; one ✗ still means REQUEST CHANGES, but list every blocking issue of that pass. Замінити приклад RC «### Issues Found» (2 булети) і рядок «Fix the above, then re-run `/opsx:review <name>`» на обов’язкові секції `Checklist` (кожен пункт T2 ✓/✗), `Findings` (Blocker / Major / Minor, порожні відра дозволені), `Required Before Apply` (лише blocking), `Previous findings`. Заголовок `Previous findings` ALWAYS присутній після будь-якого Tier 2 проходу. Якщо попереднього `review.md` не було — тіло є літеральним рядком `none — first review cycle`. Якщо був — кожен попередній пункт Required Before Apply → `resolved` | `unresolved` плюс однорядкове evidence. Re-review MUST scan the same defect class — лише LLM-only (інший таск, чий `Do:` не виконується без design.md; інша design-поведінка без вимоги в delta; інший drift proposal↔tasks; інший згаданий заголовок/шлях, якого немає); класи Tier 1 NEVER входять у цей rescan; MUST NOT emit a one-item RC that names only the first leftover. Після T2 conductor MUST reject an RC `review.md` that lacks those headings or has an empty Checklist і MUST NOT rewrite the file; then re-spawn `spec-reviewer` once with the rejection reason and the required headings; if the second file is still non-conforming, close the session with `## Blocked` naming the missing headings and next command `/opsx:review <name>` (a rejected file is not an accepted verdict). Session Exit: після прийнятого REQUEST CHANGES один рядок містить `Verdict: REQUEST CHANGES` і `/opsx:propose`; після APPROVE `/opsx:apply <name>`. NEXT-AFTER-RC applies only to an accepted (schema-conforming) RC.
  Done-when: `rg -n "MUST NOT stop at the first blocking|none — first review cycle|\\*\\*Source:\\*\\* gate-check|paste the full Tier 2 checklist" templates/.agents/commands/opsx-review.md` знаходить усі чотири фрази; `rg -n "Verdict: REQUEST CHANGES.*/opsx:propose" templates/.agents/commands/opsx-review.md` знаходить принаймні один рядок; `rg -n "accepted exception|re-spawn \`spec-reviewer\` once|MUST NOT rewrite" templates/.agents/commands/opsx-review.md` знаходить усі три; файл досі містить «do NOT spawn \`spec-reviewer\`» і «do NOT read the artifacts» на гілці падіння Tier 1; немає рядка «then re-run \`/opsx:review\`» як next command після RC

- [x] 1.2 Ті самі правила в spec-reviewer.md
  Files: templates/.agents/subagents/spec-reviewer.md
  Do: Додати в Workflow/Rules нормативні речення спеціаліста (не parent heading check і не Session Exit): MUST NOT stop at the first blocking finding; finish the full LLM checklist and complete artifact scan before the verdict; REQUEST CHANGES / re-review MUST use headings Checklist, Findings (Blocker / Major / Minor), Required Before Apply, Previous findings. Заголовок `Previous findings` ALWAYS присутній після будь-якого Tier 2 проходу. Якщо попереднього `review.md` не було — тіло є літеральним рядком `none — first review cycle`. Якщо був — кожен попередній пункт Required Before Apply → `resolved` | `unresolved` плюс однорядкове evidence. Додати буквальну вимогу: read the existing `review.md` before overwriting it. Cosmetics stay out of Required Before Apply. Later review MUST carry prior Required Before Apply into Previous findings and MUST still scan the same defect class — лише LLM-only (інший таск, чий `Do:` не виконується без design.md; інша design-поведінка без вимоги в delta; інший drift proposal↔tasks; інший згаданий заголовок/шлях, якого немає); класи Tier 1 NEVER входять у цей rescan; MUST NOT emit a one-item RC that lists only the first leftover. Не прибирати read-only обмеження і правило apply-notes.md лише на APPROVE.
  Done-when: `rg -n "MUST NOT stop at the first blocking|Previous findings|same defect class|read the existing \`review.md\` before overwriting" templates/.agents/subagents/spec-reviewer.md` знаходить усі чотири фрази; файл містить «none — first review cycle»; файл досі містить «apply-notes.md» і заборону редагувати `tasks.md`

## 2. Propose і маршрутизація

- [x] 2.1 Punch list і T1-only виняток у propose
  Files: templates/.agents/commands/opsx-propose.md, templates/.agents/subagents/spec-architect.md, templates/.agents/skills/openspec-propose/SKILL.md
  Do: Після існуючого абзацу task-contract додати блоки з різними суб’єктами. У `templates/.agents/commands/opsx-propose.md` і `templates/.agents/skills/openspec-propose/SKILL.md`: on re-propose after `review.md` Verdict REQUEST CHANGES, the conductor MUST pass `review.md` (path + verdict + Required Before Apply list) in the `spec-architect` spawn prompt and verify the report addresses every item; the parent MUST NOT itself edit proposal/design/specs/tasks. У `templates/.agents/subagents/spec-architect.md`: the architect MUST read `review.md`, fix every Required Before Apply item, and re-scan the same defect class in proposal.md, design.md, tasks.md, and all delta specs (LLM-only classes only: another task whose `Do:` is not executable without design.md; another design behaviour with no delta requirement; another proposal↔tasks drift; another referenced heading/path that does not exist); do not stop after the listed items; Tier 1 classes NEVER enter this rescan. Exception in all three files: the structure-only propose trigger is the exact line `**Source:** gate-check` plus the absence of `## Checklist`; then fix only those gate-check errors. Не змінювати вимогу Files/Do/Done-when і mandatory spawn `spec-architect`. Існуюче речення «The parent MUST NOT create or edit proposal/design/specs/tasks» у двох conductor-файлах залишити.
  Done-when: `rg -n "path \\+ verdict \\+ Required Before Apply" templates/.agents/commands/opsx-propose.md templates/.agents/skills/openspec-propose/SKILL.md` дає щонайменше по одному збігу в кожному з двох conductor-файлів; `rg -n "fix every Required Before Apply" templates/.agents/subagents/spec-architect.md` знаходить фразу; `rg -n "\\*\\*Source:\\*\\* gate-check" templates/.agents/commands/opsx-propose.md templates/.agents/subagents/spec-architect.md templates/.agents/skills/openspec-propose/SKILL.md` дає щонайменше по одному збігу в кожному з трьох; усі три досі містять «Done-when»

- [x] 2.2 Routing RC → propose в openspec-guide
  Files: templates/.agents/subagents/openspec-guide.md
  Do: Замінити лише рядок 18 (`proposal.md` exists but no `review.md` with `Verdict: APPROVE` → `/opsx:review <name>` (must run in a separate read-only session)) двома гілками: no `review.md` → `/opsx:review <name>`; `review.md` містить `Verdict: REQUEST CHANGES` → `/opsx:propose <name>` (у гілці RC `Verdict: REQUEST CHANGES` і `/opsx:propose` стоять в одному рядку). Рядок 19 (`review.md` says APPROVE but `tasks.md` has unchecked `- [ ]` items → `/opsx:apply <name>`) залишити єдиною гілкою APPROVE→apply і лише перефразувати «says APPROVE» на `Verdict: APPROVE`. Не додавати друге правило APPROVE→apply. Видалити формулювання, яке мапить будь-який non-APPROVE review.md знову на review.
  Done-when: `rg -n "Verdict: REQUEST CHANGES.*/opsx:propose" templates/.agents/subagents/openspec-guide.md` знаходить принаймні один рядок; `rg -n "no \`review.md\` with \`Verdict: APPROVE\`" templates/.agents/subagents/openspec-guide.md` не знаходить старого правила; `rg -n "says APPROVE" templates/.agents/subagents/openspec-guide.md` порожній; файл містить одну гілку \`Verdict: APPROVE\` → \`/opsx:apply\` (перефразований колишній рядок 19) і гілку «немає review.md → /opsx:review»; немає другого правила APPROVE→apply

## 3. Метрика, тести, changelog

- [x] 3.1 Метрика ≤ 2 і анти-патерн
  Files: templates/.agents/skills/agent-orchestration/SKILL.md, templates/AGENTS.md
  Do: У skill замінити рядок «Spec review loops: ≤ 1» (зараз біля Metrics) на «Spec review discovery loops: ≤ 2» і додати речення, що confirmation APPROVE після вичерпного propose не рахується discovery loop. Замінити «If Request Changes — fix artifacts, re-run `/opsx:review`.» на «If Request Changes — run `/opsx:propose <name>` to fix the punch list, then a new `/opsx:review`.». Додати в таблицю Anti-patterns рядок «one-finding review loop» з impact «fragments defects across many propose/review sessions». У `templates/AGENTS.md` після абзацу «Quality gates: `gate-check --tasks <name>` lints the task contract» додати два речення: Spec review discovery loops ≤ 2 і анти-патерн one-finding review loop.
  Done-when: `rg -n "run \`/opsx:propose <name>\` to fix the punch list|Spec review discovery loops: ≤ 2|one-finding review loop" templates/.agents/skills/agent-orchestration/SKILL.md` знаходить усі три; `rg -n "Spec review loops: ≤ 1" templates/.agents/skills/agent-orchestration/SKILL.md` порожній; `rg -n "Spec review discovery loops ≤ 2|one-finding review loop" templates/AGENTS.md` знаходить обидві фрази

- [x] 3.2 Smoke string-match на нові MUST-фрази
  Files: test/smoke.test.js
  Do: Після існуючого `test('opsx-review writes review.md and vue3 checklist'` (зараз біля рядка 628) додати новий `test('…')` у тому ж стилі: `readFileSync` шаблонів з 1.1–3.1 і `assert.match` / `assert.doesNotMatch` на фрази «MUST NOT stop at the first blocking», «Previous findings», «same defect class», «Required Before Apply», однорядкове `Verdict: REQUEST CHANGES` + `/opsx:propose` у `opsx-review.md` і `openspec-guide.md` (regex на кшталт `/Verdict: REQUEST CHANGES.*\/opsx:propose/`), «run `/opsx:propose <name>` to fix the punch list» у skill, «Spec review discovery loops: ≤ 2» (smoke regex MUST match this skill form with the colon; do not assert the AGENTS.md form without the colon), «one-finding review loop» у skill, `**Source:** gate-check` у `opsx-review.md`, відсутність «Spec review loops: ≤ 1» і відсутність старого guide-правила «no review.md with Verdict: APPROVE» як єдиного non-APPROVE → review. Існуючий тест на `review.md` і `Vue 3` у `opsx-review.md` не видаляти. Не додавати CLI-гейт і не додавати асерт, що рахує кількість findings у `review.md`.
  Done-when: у `test/smoke.test.js` є асерти `MUST NOT stop at the first blocking`, `Previous findings`, `Spec review discovery loops: ≤ 2`, `one-finding review loop`, `Verdict: REQUEST CHANGES.*\\/opsx:propose`, `run \`/opsx:propose <name>\` to fix the punch list`; немає нового виклику `gate-check` і немає порівняння `findings.length`; існуючий тест `opsx-review writes review.md and vue3 checklist` лишається

- [x] 3.3 CHANGELOG [Unreleased]
  Files: CHANGELOG.md
  Do: У порожню секцію `## [Unreleased]` додати пункт Changed: kit templates вимагають вичерпний Tier 2 punch list (повний скан до вердикту, схема Checklist / Findings / Required Before Apply / Previous findings), після REQUEST CHANGES next command є `/opsx:propose`, `openspec-guide` більше не мапить RC на повторний review, метрика Spec review discovery loops ≤ 2. Не змінювати номер версії в `package.json`.
  Done-when: `rg -n "Unreleased|punch list|/opsx:propose|discovery loops" CHANGELOG.md` показує ці фрази під `## [Unreleased]`; `package.json` version рядок не змінений цим таском
