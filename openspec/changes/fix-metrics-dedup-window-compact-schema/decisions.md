# Decisions — fix-metrics-dedup-window-compact-schema

<!-- append-only; пише npx agent-orchestrator-kit handoff <name> з handoff.md ## Decisions -->

- 2026-09-07 session-v2: сесія зберігає агрегати + sourceIds + sourceTotals + byModel замість per-message sources; version 2; v1 мігрує на першому записі, `metrics --migrate` для архівів
- 2026-09-07 persist-window: без pending і без --started-at нижня межа = last.endedAt (або createdAt); pre-restore grace 120s з dedup; persist/archive без listRecentAmpThreadIds; локальні Amp threads без thread id не скануються; Cost треда не білиться двічі
- 2026-09-07 claude-adapter: dedup за message.id, subagents/*.jsonl, prefix cwd, estimate за Anthropic list-price з cache-split
- 2026-09-07 leftover-cap: для threadId null leftoverEnd = min(next pending, endedAt+120s); Cursor threadId з env на persist без pending
- 2026-09-07 amp-usage-truth: session totals з usage, модель чату з export, свіжий Cost на leftover, fallback estimate без Cost
- 2026-09-07 dashboard-contract: `metrics --summary-json` + README «Для дашбордів»; межі фаз лише з phases.*, git log заборонено, commits per phase не постачаємо
- 2026-09-07 spawn-name: лише backtick або карта ролей; rule-text: --no-metrics для перегенерації, без повторного persist, decisions.md лише CLI, стоп після persist, platform/model обов'язкові
- 2026-09-07 review-verdict: REQUEST CHANGES; наступний крок — Architect править артефакти (не код) і повертає на повторний `/opsx:review`; review.md перезаписується наступним reviewer-проходом
- 2026-09-07 fix-order: спершу B1 (переписати два v1-сценарії startedAt + у 6.1 явно переписати smoke 'earliest source.at' і ~95 асертів `sessions[].sources`), потім M4 (додати в MODIFIED усі main-вимоги, що нормативно згадують `session.sources`, замінити на `sourceIds`/`byModel`), M2 (leftover-resync Amp: якщо usage дав totals — totals з usage, не сума подій), M3 (`costSource` у формі `byModel[]`), M5/M6 (2.1 → `metricsFinalizeArchive` + `metricsBackfillFile`; окремий таск для readers `.sources`: sessionFieldOrSources, leftoverAmpThreadId, uniqueAmpSourceThreadPrefix, sessionAmpThreadKeys, sessionTotalsLookOverridden), M7 (5.2 не копіювати kit→templates байт-у-байт; бюджет always-apply rule 11 607/12 000 — новий абзац лише з trimming), M1 (Вікно collect: prefix-match cwd для Claude)
- 2026-09-07 architect-revision: B1/M1–M7/minor/nit artifacts revised; no approval inferred; return to /opsx:review in a NEW chat.
- 2026-09-07 compact-spec-coverage: 24 modified metrics requirements include --no-metrics, persisted v2 fields sourceIds/sourceTotals/byModel, and distinguish transient collectors from explicit legacy fixtures; 18 tasks remain unchecked.
- 2026-09-07 migration-invariance: schema-only --migrate preserves session numerics and all existing spend/totals/platform/model/phase aggregates without recompute or consumer numeric backfill; subsequent real writes recompute normally.
- 2026-09-07 review-verdict: REQUEST CHANGES remains historical until independent re-review; the reviewer overwrites review.md and writes apply-notes.md only on APPROVE.
- 2026-09-07 review-rereview: independent Tier 2 re-review confirms B1/M1–M7/m1–m4 resolved; verdict REQUEST CHANGES solely for new Major N1; review.md replaced in English; no apply-notes.md; no approval inferred.
- 2026-09-07 archive-collect-window: every requirement and scenario for Archiver `archive --collect` MUST use the lower bound `[pending.startedAt − 120s, now]` with dedup, and events before that grace bound MUST NOT enter the Archiver session; fix the MODIFIED `--collect` requirement (delta :972) and its Archiver-window scenario (:1018–1021) to match :550, proposal.md:13, and tasks.md 2.1. Architect edits artifacts only, then a NEW `/opsx:review` chat.
- 2026-09-07 n1-archive-collect-window-fixed: delta change-metrics spec now states for archive --collect: leftover of the previous session first (leftoverEnd = pending.startedAt), then all three adapters in [pending.startedAt − 120s, now] with dedup by id against sourceIds of all sessions; events before pending.startedAt − 120s MUST NOT enter the Archiver session; events in [last.endedAt, pending.startedAt) already claimed by leftover stay out of Archiver sourceIds via dedup; lines 608/972/1019/1022 aligned with :550, proposal.md:13, tasks.md 2.1; no other semantics changed; no approval inferred — NEW /opsx:review chat required.
- 2026-09-07 review-n2-started-at-grace: REQUEST CHANGES; N1 resolved; new Major N2 — design.md:25 and tasks.md:37 must express the collect lower bound by start provenance, e.g. `windowStart = (--started-at supplied || pending) ? startedAt − 120000 : startedAt`, so explicit `--started-at` without pending gets the 120-second grace exactly as delta spec :335/:339 requires; Architect edits artifacts only (design.md, tasks.md task 2.1; no spec change expected unless wording drifts), then a NEW `/opsx:review` chat; review.md is replaced only by the next independent reviewer pass; no approval inferred.
- 2026-09-07 review-n2-started-at-grace: reviewer REQUEST CHANGES for Major N2 (grace formula keyed on pending instead of start provenance). n2-started-at-grace-fixed: design.md:25 and tasks.md:37 rewritten to `windowStart = (startedAt from --started-at || pending.startedAt) ? startedAt − 120s : startedAt`; matches delta :335/:339; no other semantics changed; fix applied in the review chat on explicit owner instruction; no approval inferred — NEW /opsx:review chat required.
- 2026-09-07 review-approve-n2-n1-verified: independent Tier 2 spec-reviewer verdict APPROVE for fix-metrics-dedup-window-compact-schema; N2 (grace by start provenance at design.md:25 / tasks.md:37, matching delta :335/:339) and N1 (Archiver window [pending.startedAt − 120s, now], leftover-first, dedup) verified resolved; all `120` occurrences classified without drift; apply-notes.md written; apply may start in a NEW `/opsx:apply` chat; review.md must not be edited by the implementer.
- 2026-09-07 `review-approve-n2-n1-verified`: the approved change remains ready for apply; preserve the reviewed N2/N1 requirements and do not edit review.md during implementation.
- 2026-09-07 `code-review-premature-no-diff`: the requested post-implementation reviewer found no implementation diff, so code review cannot proceed and apply remains the next phase.
- 2026-09-07 Preserve compact persisted sessions (`sourceIds`, `sourceTotals`, `byModel`) and keep per-event `sources` transient only.
- 2026-09-07 Preserve existing billed Amp Cost unless fresh leftover usage supplies Cost; do not bill an already-recorded thread again in a new session.
- 2026-09-07 Keep schema-only migration numerically invariant and free of recompute.
